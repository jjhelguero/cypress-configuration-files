{
  "scripts": {
    "cy:run:dev": "npx cypress run --env configFile=dev,grepTags=@dev,grepOmitFiltered=true",
    "cy:run:qa": "npx cypress run --env configFile=qa,grepTags=@qa,grepOmitFiltered=true",
    "cy:run:prod": "npx cypress run --env configFile=prod,grepTags=@prod,grepOmitFiltered=true"
  },
  "devDependencies": {
    "cypress": "9.2.1",
    "cypress-grep": "3.0.2"
  }
}
